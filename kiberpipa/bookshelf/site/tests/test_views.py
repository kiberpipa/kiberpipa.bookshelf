from ...testing import BaseUnitTest


class ViewsTest(BaseUnitTest):
    def test_index(self):
        pass
        #from ..views import index
        #info = index(self.request)
        #self.assertEqual(info, {})

# TODO: test if no results
# TODO: test one, zero and multiple facet selection
